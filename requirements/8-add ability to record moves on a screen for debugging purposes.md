When I press Shift + D, my screen becomes slightly blurred, but I can still recognize all the cars. A message appears at the top of the screen that says "Select interesting area." I want to record the positions of the cars visible within this area during the recording session.

Once I define the selection area (by releasing the mouse), the recording of the cars' movements begins. Pressing Esc does not trigger any action, while pressing Enter saves the recorded data to the clipboard. If I press Esc when I selection, it action cancels as well.

The goal is to use this information to improve the algorithms for car movement and physics, so it is crucial to store all relevant states with simple timestamps (in milliseconds from the start). I anticipate short recording sessions, but they might generate a lot of data, so the format should be simple and minimalistic - preferably plain text.
